$("#comment_<%= @commentable.id %> .new_comment").first()
                                                 .html("<%= j render 'form', commentable: @commentable, top_level: false %>")
                                                 .find("form .hide")
                                                 .click(function() {
                                                   $(this).parent().empty();
                                                 });
