$("#comment_<%= @commentable.id %> .new_comment").first()
                                                 .html("<%= j render 'form', commentable: @commentable %>")
                                                 .append("<%= j content_tag 'a', 'hide', class: 'hide' %>")
                                                 .children(".hide")
                                                 .click(function() {
                                                   $(this).parent().empty();
                                                 });
