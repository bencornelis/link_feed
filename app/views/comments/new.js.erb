$formDiv = $("#comment_<%= @parent_id %> .comment_form").first();
$formDiv.html("<%= j render 'form', top_level: false %>")
        .find("form .hide")
        .click(function() {
          $(this).parent().empty();
        });
