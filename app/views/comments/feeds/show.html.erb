<% content_for :sub_nav do %>
  <%= render "navs/feed" %>
<% end %>

<ul class="comments">
  <% @comments.each do |comment| %>
    <%= render "comments/comment_blurb", comment: comment %>
  <% end %>
</ul>

<%= will_paginate @comments, page_links: false, class: "digg_pagination" %>
