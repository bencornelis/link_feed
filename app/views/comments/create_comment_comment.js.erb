$parent = $("#comment_<%= @comment.parent_id %>");
$parent.find(".comment_form").first().empty();
$parent.next("ul.comments")
       .first()
       .prepend("<%= j render 'comment', comment: @comment %>");
