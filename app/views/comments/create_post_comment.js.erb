$("#post_main").children("#new_comment")
               .replaceWith("<%= j render 'new_comment_form', commentable: @commentable, top_level: true %>");
$(".comments").first().prepend("<%= j render 'comment', comment: @comment %>");
