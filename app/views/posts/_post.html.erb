<%= content_tag_for :li, post do %>
  <h4><%= post_link(post) %></h4>
  <div class="post_info">
    <div class="author">
      <%= user_link(post.user) %>
    </div>
    <div class="comment_stats">
      <%= comments_link(post) %> / <%= tag_links(post) %>
    </div>
  </div>
<% end %>
